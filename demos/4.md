# Use an agent CLI

## Opencode
zsh -l -c 'opencode run --model "github-copilot/claude-haiku-4.5" "{{ $json.chatInput }}"'

zsh -l -c '{{ $json.chatInput }}'

opencode run --model "github-copilot/claude-haiku-4.5" ""


zsh -l -c 'copilot -p "{{ $json.chatInput }}"'